#include <stdio.h>
#include <stdlib.h>

int main()
{
    int first = 1;
    int end = 100;
    int num_of_terms = 100 ;

// the formula for the sum of an arithmetic series = (num of terms /2) * (first + end )

     int sum  =(num_of_terms /2) * (first + end )   ;



    printf(" The sum numbers from 1 to 100 is : %d\n", sum);

    return 0;
}



